apiVersion: v1
kind: Service
metadata:
  name: dofuner
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dofuner"
    port: 8080
  selector:
    app: dofuner
