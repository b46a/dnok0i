apiVersion: v1
kind: Service
metadata:
  name: mkijngg
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "mkijngg"
    port: 8080
  selector:
    app: mkijngg
