apiVersion: v1
kind: Service
metadata:
  name: ruyiij
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ruyiij"
    port: 8080
  selector:
    app: ruyiij
